VITE_NBX_GATEWAY_URL=http://localhost:8080
# Opcional: se definido, pula POST /auth/login e usa Bearer fixo (ex.: pipelines).
VITE_NBX_ACCESS_TOKEN=
# POST /api/v1/interactions sempre usa customerId UUID ...0002 e channel ECOMMERCE no código da demo.
# Login via POST /api/v1/auth/login:
VITE_NBX_LOGIN_USERNAME=demo
VITE_NBX_LOGIN_PASSWORD=demo
# Opcional: quando definido, os 4 cards principais são preenchidos pela decisão (recommendedActions).
VITE_NBX_DECISION_ID=
